# 代码-论文一致性核查最终报告

**日期**: 2026-07-26  
**核查范围**: 第三章（方法）和第四章4.1节（实验设置）  
**核查类型**: 参数设置、关键公式、业务逻辑

---

## 执行摘要

✅ **已验证**: 10/11 参数一致  
❌ **发现不一致**: 1个关键参数  
⚠️ **严重程度**: 高 - 影响模型容量和可重现性

---

## 1. 参数验证结果

### ✅ **一致的参数** (10/11)

| 参数 | 论文 | 代码 | 训练日志 | 状态 |
|------|------|------|----------|------|
| G (grid size) | 64 | 64 | - | ✓ |
| L (Fourier layers) | 4 | 4 | - | ✓ |
| m1, m2 (modes) | 16 | 16 | - | ✓ |
| λ_m | 100 | 100 | - | ✓ |
| λ_p | 1.0 | 1.0 | - | ✓ |
| lr | 0.001 | 0.001 | - | ✓ |
| batch_size | 1 | 1 | - | ✓ |
| epochs | 200 | 200 | - | ✓ |
| γ (LR decay) | 0.995 | 0.995 | - | ✓ |
| optimizer | AdamW | AdamW | - | ✓ |

### ❌ **不一致的参数** (1/11)

| 参数 | 论文声称 | 代码默认值 | 实际训练值 | 差异 |
|------|----------|------------|------------|------|
| **W (FNO width)** | **32** | **48** | **48** | **+50%** |

**证据**:
1. 论文 (Section 4.1, Line 582): "W=32"
2. 代码 (trainer.py:3535): `--hidden_channels default=48`
3. 训练日志: `hidden_channels = 48`
4. 代码逻辑 (models.py:641): `width=max(16, interm_channels)` → 48

---

## 2. 公式验证结果

### ✅ **Equation 3: FNO输入构造**

**论文**:
```
inp(x,y) = [g(x,y), g(x,y), x̂, ŷ, f/f_max]
```

**代码** (models.py:183-186):
```python
inp = torch.stack([gauss, gauss, coord[0], coord[1], fmap], dim=-1)
```

**状态**: ✅ **完全匹配**

### ✅ **Equation 7: Fourier层**

**论文**:
```
x^(l+1) = σ(W^(l)·x^(l) + F^(-1)[R^(l)·F[x^(l)]])
```

**代码** (models.py:158-159):
```python
for spc, w in zip(self.specs, self.ws):
    x = F.gelu(spc(x) + w(x))
```

**状态**: ✅ **架构匹配**

### ✅ **Equation 6: 输出投影**

**论文**:
```
u_p = [corr_r ; corr_i] ∈ R^(2N)
```

**代码** (models.py:161-166):
```python
x = self.proj2(F.gelu(self.proj1(x)))  # → [B,H,W,2]
samp = F.grid_sample(x, grid, mode="bilinear")  # → [B,N,2]
```

**状态**: ✅ **完全匹配**

---

## 3. 业务逻辑验证

### ✅ **核心逻辑一致**

| 逻辑项 | 状态 |
|--------|------|
| 5通道输入 | ✓ |
| 高斯源图 exp(-r²/2σ²) | ✓ |
| σ = 0.05×max(Lx,Ly) | ✓ |
| 频率归一化 f/100 | ✓ |
| 双线性采样 G×G→N | ✓ |
| 实数化输出 [real;imag] | ✓ |
| L=4 Fourier层 | ✓ |
| 模态截断 m1=m2=16 | ✓ |

---

## 4. 影响分析

### **模型容量差异**

| 指标 | W=32 (论文) | W=48 (实际) | 差异 |
|------|-------------|-------------|------|
| FNO参数量 | ~32K | ~72K | **+125%** |
| 计算复杂度 | 基准 | 2.25×基准 | **+125%** |
| 内存占用 | 基准 | 1.5×基准 | **+50%** |

### **对研究的影响**

1. **精度报告**: 论文报告的精度基于更大的模型（W=48）
2. **可重现性**: 读者按W=32训练会得到**更差的结果**
3. **公平性**: 与基线对比可能不完全公平
   - FNO基线使用 W=32 (trainer.py:3446硬编码)
   - 提出方法实际使用 W=48

---

## 5. 修正建议

### **🎯 推荐方案: 修改论文**

**修改位置**: Section 4.1, Line 582

**原文**:
> The FNO prior uses G=64, **W=32**, L=4 Fourier layers, and...

**修改为**:
> The FNO prior uses G=64, **W=48**, L=4 Fourier layers, and...

**理由**:
- ✅ 所有实验结果基于W=48
- ✅ 无需重新训练
- ✅ 训练日志已确认
- ✅ 修改最小，风险最低

### **替代方案: 代码回退到W=32**

**不推荐**，因为需要：
- 重新训练所有42个案例
- 可能导致精度下降
- 重新生成所有表格和图像
- 时间成本高

---

## 6. 验证清单

- [x] 检查代码默认参数
- [x] 检查训练日志实际值
- [x] 追踪参数传递路径
- [x] 验证公式实现
- [x] 分析影响范围
- [ ] **待办**: 修改论文参数描述
- [ ] **待办**: 检查其他节是否提到W=32
- [ ] **待办**: 更新supplementary material

---

## 7. 总体结论

### **✅ 核心架构和逻辑完全一致**

- 所有关键公式正确实现
- 业务逻辑与论文描述匹配
- 训练流程符合描述

### **❌ 一个参数描述错误**

- FNO宽度: 论文W=32 vs 实际W=48
- 需要修正论文描述

### **推荐行动**

1. **立即**: 修改论文Section 4.1: W=32 → W=48
2. **检查**: 全文搜索"32"，确保没有其他地方错误引用
3. **更新**: Supplementary material中的参数表
4. **说明**: 在revision notes中说明此修正

---

## 附录：代码追踪路径

```
论文声称: W=32
    ↓
trainer.py Line 3535: --hidden_channels default=48
    ↓
trainer.py Line 3429: interm_channels=args.hidden_channels (=48)
    ↓
models.py Line 935: def __init__(..., interm_channels=32, ...)
                    # 默认32被覆盖为48
    ↓
models.py Line 641: width=max(16, self.interm_channels)
                    # = max(16, 48) = 48
    ↓
models.py Line 103: def __init__(..., width: int = 32, ...)
                    # 默认32被覆盖为48
    ↓
训练日志确认: hidden_channels = 48
    ↓
实际FNO宽度: W = 48 ≠ 32 (论文)
```

---

**报告完成时间**: 2026-07-26  
**优先级**: 🔴 高 - 投稿前必须修正  
**责任**: 论文作者需要立即确认并修正
